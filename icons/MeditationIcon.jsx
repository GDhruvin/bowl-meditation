import SimpleLineIcons from "react-native-vector-icons/SimpleLineIcons";

export default function MeditationIcon({ color, size }) {
  return <SimpleLineIcons name="emotsmile" color={color} size={20} />;
}
